import React from "react";
import "./skillsitem.css";
const educationitem = (props) => (
  <img class="skills_tools-logo" src={props.filepath} alt={props.alttext} />
);

export default educationitem;
